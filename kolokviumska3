/*
Од стандарден влез се читаат знаци се додека не се прочита извичник. Во вака внесениот текст се 
скриени цели броеви (помали од 100). Да се напише програма што ќе ги прочита сите знаци и на излез
ќе го испечати збирот на сите броеви скриени во текстот.
*/
#include <stdio.h>
#include <stdlib.h>
int main()
{
    char c;
    int n=0,i,prev=0;
    scanf("%c",&c);
    while(c!='!') 
    {
        if(c<='9'&&c>='0')
        {
            i = c - '0'; 
            if(prev==0){ 
                prev += i;
            }
            else if(prev!=0){ 
                n += prev*10 + i;
                prev = 0;
            }
        }
        else{ 
            n+=prev;
            prev=0;
        }
        scanf("%c",&c);
    }
    if(c=='!')
    {
        n+=prev;
        prev=0;
    }
    printf("%d",n); 
    return 0;
}
