/*
Да се напише програма која вчитува матрица со димензии MxN (макс. 100x100). На почетокот 
се внесуваат димензиите на матрицата, а потоа и елементите на матрицата кои се само вредностите 1 и 0. 
Програмата треба да изброи и отпечати на СИ во колку од редиците и колоните има барем 3 последователни елементи со вредност 1.
*/
#include <stdio.h>
int main(){
int a[100][100],i,j,n,m;

scanf("%d %d", &n, &m);

for(i=0; i<n; i++){
    for(j=0; j<m; j++){
        scanf("%d", &a[i][j]);
    }
}
int redica=0; int pp=0;
for(i=0; i<n; i++)
{
    for(j=0; j<m; j++){

        if(a[i][j] ==1 && a[i][j+1] == 1 && a[i][j+2]==1 && pp==0){
            redica++;
            pp=1;
        }
    }
    pp=0;
}
int prethodno=0;
for (j = 0; j < m; j++)
{
    for (i = 0; i < n; i++)
    {
        if(a[i][j] ==1 && a[i+1][j] == 1 && a[i+2][j]==1 && prethodno==0){
            redica++;
            prethodno=1;
        }
    }
    prethodno=0;
}

printf("%d", redica);

return 0;

}
